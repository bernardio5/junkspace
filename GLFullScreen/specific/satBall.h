/* maintains "numSat" satellites also causes them to be displayed.   */#import <Cocoa/Cocoa.h>#import "satellite.h"#import "SGManager.h"typedef struct {    int index;    float depth;} satDepthStruct;@interface satBall : NSObject {    satellite *sats[NUM_SATS];     int satInds[NUM_SATS];    sgManager *theSG;     int maxSats;         int lastCount, loaded;     int oldClosest;     int switcher, stripeCt;}- (void)setup:(sgManager*)aSG;  - (void)takeName:(NSString*)n; - (void)addSat:(satInitRec*)newbie; - (void) update:(double)yearFract ;// if a call to hasNewName rets 1, call "getName"- (int)hasNewName:(mVec3*)ground:(mVec3*)dir;- (NSString*)getName; - (int)getID;@end 