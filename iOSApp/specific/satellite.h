/* elliptical orbits  */#import <Foundation/Foundation.h>#import "sprite.h"#import "mVec3.h"#import "mx4.h"#define NUM_SATS 11475typedef struct {    // line 2    int objectID;     double epoch; // year, day and hour of observation    double epochYear;    double dmdt; // 1st dervative of motion wrt time    double dmdtdt; // 2nd deriv    double bStar; // B* drag term        // line 3    double OID; // orbital inclination in degrees    double RAAN; // right ascension of ascending node (degrees)    double eccentricity; // orbit eccentricity (decimal point assumed)    double ArgPerg; // argument of perigee (degrees)    double MeanAnomaly; // (degrees)    double MeanMotion; // revolutions about the earth per day    int RevNumber; // number of times around the earth it's been as of Epoch    } satInitRec; @interface satellite: NSObject {    // line 2    int objectID;     double epoch; // year, day and hour of observation    double epochYear;    double dmdt; // 1st dervative of motion wrt time    double dmdtdt; // 2nd deriv    double bStar; // B* drag term        // line 3    double OID; // orbital inclination in degrees    double RAAN; // right ascension of ascending node (degrees)    double eccentricity; // orbit eccentricity (decimal point assumed)    double ArgPerg; // argument of perigee (degrees)    double MeanAnomaly; // (degrees)    double MeanMotion; // revolutions about the earth per day    int RevNumber; // number of times around the earth it's been as of Epoch        // convenience    double T, a, b, ae, radsPerDay;    mx4 perigXF, oidXF, raanXF;         mVec3 pos; // the position in space wrt the standard earth orientation    double rot, dr;    NSString *name; }- (void) reset:(satInitRec *)it;- (void) update:(double)yearFract;- (void) getPos:(mVec3 *)pout; - (void) takeName:(NSString*)n; - (NSString*) getName; - (int)getID; @end